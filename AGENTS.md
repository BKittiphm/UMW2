# Working Agreement for Humans and AI

ไฟล์นี้เป็นจุดเริ่มต้นบังคับสำหรับผู้พัฒนาและ AI ที่เข้ามาทำงานต่อในโครงการ UMW2

## 1. Required reading order

ก่อนวิเคราะห์ ออกแบบ หรือแก้ไฟล์ ให้อ่านตามลำดับ:

1. `README.md`
2. `CONTEXT.md`
3. `PROJECT-STATE.md`
4. เอกสาร requirement หรือ legacy baseline ของโมดูลที่กำลังทำ
5. เอกสาร architecture และ ADR ที่เกี่ยวข้อง
6. checkpoint ล่าสุดใต้ `docs/aegis/work/` เมื่อกำลัง resume งานที่ค้างอยู่
7. `docs/memory/README.md` และบันทึกการคุยล่าสุด เพื่อเข้าใจเหตุผลและข้อเสนอที่ยังไม่อนุมัติ

ห้ามเริ่มจากความจำของบทสนทนาเพียงอย่างเดียว

## 2. Authority order

เมื่อข้อมูลขัดกัน ให้ใช้ลำดับต่อไปนี้:

1. คำยืนยันล่าสุดโดยตรงจากเจ้าของโครงการ
2. ADR ที่มีสถานะ `Accepted`
3. เอกสาร target architecture/requirements ที่ได้รับอนุมัติ
4. `CONTEXT.md` สำหรับความหมายของคำศัพท์
5. `PROJECT-STATE.md` สำหรับสถานะและลำดับงาน
6. Legacy baseline สำหรับข้อเท็จจริงของระบบเดิมเท่านั้น
7. ข้อสังเกตจากหน้าจอ โค้ด หรือการคาดคะเน

หากยังตัดสินไม่ได้ ให้บันทึกเป็น Open Decision และถามเจ้าของโครงการ ห้ามเดาเป็นข้อเท็จจริง

## 3. Separate AS-IS from TO-BE

- ไฟล์ `JarTest/legacy-jar-test-product-functional-spec-th.md` เป็น **AS-IS baseline** ของระบบเดิม
- เอกสารใต้ `docs/architecture/` และ ADR เป็น **TO-BE decisions** ของระบบใหม่
- ห้ามแก้ legacy baseline เพื่อให้ดูตรงกับแบบใหม่
- หากค้นพบข้อมูลระบบเดิมเพิ่มเติม ให้เพิ่มหลักฐานและระบุระดับความมั่นใจ
- หากมีฟังก์ชันอัปเกรด ให้เขียนเป็น target requirement แยกต่างหาก

## 4. Canonical terminology

- `Business Unit` หมายถึง `กิจการประปา`
- `Site` หมายถึง `สถานี`
- ห้ามใช้ `กิจการประปา` และ `Site` เป็นสิ่งเดียวกัน
- ในระบบใหม่ใช้คำว่า `สถานี` ไม่ใช้ `สถานีผลิต` เป็นชื่อ canonical
- `Water Source` หมายถึง `แหล่งน้ำ` และเป็น Global Master
- การใช้แหล่งน้ำของแต่ละ Site ต้องผ่านความสัมพันธ์ Site–Water Source

รายละเอียดและคำที่ยังคลุมเครืออยู่ใน `CONTEXT.md`

## 5. Jar Test rules currently fixed

- เริ่มสร้างโมดูล Jar Test ก่อนโมดูลใหญ่อื่น
- ระยะแรกใช้ legacy specification เป็น acceptance baseline
- สูตรปริมาณสารละลายใช้หลัก `C1V1 = C2V2` ตามพฤติกรรมที่บันทึกไว้
- ผลผ่าน/ไม่ผ่านคำนวณจาก Bound ไม่ให้ผู้ใช้เลือกเอง
- ห้ามแต่งค่า Bound หรือสูตรแนะนำสารเคมีที่ยังไม่ทราบ
- Water Source ที่เลือกใน Jar Test ต้องเป็นรายการที่ผูกกับ Site นั้น

## 6. Safety when inspecting the legacy application

- ห้ามลบข้อมูลหรือทำ destructive action
- ห้ามแก้ข้อมูลจริงที่อยู่นอกขอบเขตการทดสอบ
- หากจำเป็นต้องสร้างหรือบันทึกข้อมูลทดสอบในระบบเดิม ให้เลือกค่าที่หน้า UI แสดงเป็น `สถานีผลิต Head Office` เท่านั้น
- บันทึกทุกการทดลองที่เปลี่ยนข้อมูล พร้อมวัตถุประสงค์และผลลัพธ์
- ห้ามคัดลอก source code, credentials, secrets หรือข้อมูลที่ไม่มีสิทธิ์เข้าถึง

## 7. Continuity protocol

ก่อนจบช่วงทำงานที่มีการตัดสินใจหรือเปลี่ยนแปลงสำคัญ:

1. ปรับ `PROJECT-STATE.md` ให้ตรงกับสถานะจริง
2. ปรับ `CONTEXT.md` เมื่อมีคำศัพท์หรือความสัมพันธ์ที่ยืนยันใหม่
3. สร้างหรือแก้ ADR เมื่อมีการตัดสินใจสถาปัตยกรรมที่คงทน
4. ปรับ requirement เมื่อพฤติกรรมผลิตภัณฑ์ได้รับการยืนยัน
5. บันทึกสิ่งที่ยังไม่รู้และ next step โดยไม่แต่งคำตอบ
   - เพิ่มหรือปรับบันทึกการคุยใน `docs/memory/sessions/` ตามรูปแบบใน `docs/memory/README.md`
   - แยกคำยืนยันของผู้ใช้ ข้อเสนอของ AI ข้อสังเกตจากเครื่องมือ และข้อมูลที่ขาด
   - ลิงก์ไปเอกสารเจ้าของข้อสรุปปัจจุบัน; ประวัติการคุยไม่แทนที่ข้อกำหนดล่าสุด
6. Commit และ push ไฟล์ที่เกี่ยวข้องก่อนย้ายเครื่อง

งานถือว่าส่งต่อได้เมื่อ AI ตัวใหม่สามารถอ่านไฟล์ตามลำดับในข้อ 1 แล้วอธิบายสถานะปัจจุบัน สิ่งที่ห้ามเปลี่ยน และงานถัดไปได้โดยไม่ต้องอ่านบทสนทนาเดิม
